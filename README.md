# test-codex-env

This repository contains toy examples for demonstrating code updates.

## Material Prediction

Run the `material_random_forest.py` script to train a toy random forest on a small
built-in dataset and output predicted hardness values for each material.

```bash
python3 material_random_forest.py
```

